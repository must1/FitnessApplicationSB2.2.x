version: "3.2"
services:
  postgres:
    image: postgres
    restart: always
    ports:
      - "5432"
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: FITNESSAPPLICATION_USER
      POSTGRES_PASSWORD: FITNESSAPPLICATION_PASS
